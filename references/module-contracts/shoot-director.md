# shoot-director contract

Input:
- `visual-gaps.json`
- requested channels/aspect ratios

Output artifact:
- `shoot-plan.json`

Required fields:
- `recommended_direction`
- `strategy_rationale`
- `shots[]` (default size 12)
