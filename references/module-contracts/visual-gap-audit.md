# visual-gap-audit contract

Input:
- `scout.json`
- `preservation.json`

Output artifact:
- `visual-gaps.json`

Required fields:
- `rows[]` with `asset`, `status`, `notes`, `priority`

Rules:
- avoid marking everything high priority
- prioritize conversion-trust shots before novelty.
